VersionLabel examples:

Hover over the component to see the branch.

```js
<VersionLabel version="4242dead" branch="master"/>
```

A dirty branch (with un-tracked or modified content) is also visible in the
tooltip.

```js
<VersionLabel version="4242dead+" branch="master"/>
```
